[project]
name = "ducklake-local-lab"
version = "0.1.0"
description = "Local DuckLake architecture experimentation with PostgreSQL, MinIO, DuckDB, and marimo"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.5.5",
    "marimo>=0.24.0",
    "pandas>=3.0.5",
    "polars>=1.44.1",
]
